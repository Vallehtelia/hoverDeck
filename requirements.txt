# HoverDeck runtime dependencies (Python 3.11+)
PyQt6>=6.6
pynput>=1.7.6
cryptography>=42.0
mss>=9.0
Pillow>=10.0
httpx>=0.27

# Windows-only helpers
pywin32>=306 ; sys_platform == "win32"
keyboard>=0.13.5 ; sys_platform == "win32"

# Dev / tooling
pytest>=8.0
pyinstaller>=6.0 ; sys_platform == "win32"
