# Windows runtime deps for HoverDeck (used by run-windows.bat / build-windows.bat).
# Mirrors pyproject.toml; pinned loosely so pip resolves current compatible wheels.
PyQt6>=6.6
pynput>=1.7.6
cryptography>=42.0
mss>=9.0
Pillow>=10.0
httpx>=0.27
psutil>=5.9
pywin32>=306
keyboard>=0.13.5
